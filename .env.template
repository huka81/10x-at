###############################################
# Plik szablonu zmiennych środowiskowych (.env)
# Skopiuj jako .env i uzupełnij wartości (usuń < >).
# Nigdy nie commituj prawdziwego klucza ani haseł.
#
# Generacja klucza Fernet (ENCRYPTING_KEY):
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Klucz musi być 32‑bajtowym, base64 url‑safe stringiem.
###############################################

# Database Configuration
# Host serwera PostgreSQL (np. localhost lub adres VPC)
DB_HOST=<YOUR_DB_HOST_HERE>
# Port PostgreSQL (domyślnie 5432)
DB_PORT=<YOUR_DB_PORT_HERE>
# Nazwa bazy danych (np. aivestor_at)
DB_NAME=<YOUR_DB_NAME_HERE>
# Użytkownik z uprawnieniami do migracji i CRUD
DB_USER=<YOUR_DB_USER_HERE>
# Zaszyfrowane hasło użytkownika (użyj encrypt_password z python/tools/encryption.py)
DB_PASSWORD=<YOUR_DB_ENCRYPTED_PASSWORD_HERE>
# Klucz Fernet do szyfrowania/dekodowania haseł i sekretów
ENCRYPTING_KEY=<YOUR_ENCRYPTING_KEY_HERE>

# Application Configuration
# Środowisko uruchomieniowe: development | staging | production
APP_ENV=development
# Włącza dodatkowe logowanie/debug; ustaw False w production
DEBUG=True
# Ścieżka bazowa do katalogu logów (upewnij się że istnieje / ma prawa zapisu)
LOGS_PATH=~\logs

# Zalecenia bezpieczeństwa:
# - Użyj osobnych użytkowników DB per środowisko (zasada najmniejszych uprawnień)
# - Rotuj ENCRYPTING_KEY przy zmianie cyklu haseł
# - Nie przechowuj haseł w formie jawnej; zawsze szyfruj
# - Sprawdź uprawnienia katalogu logów (brak dostępu dla innych użytkowników systemu)
